# rxtriage-slm-demo
Fine-tuned 1–3B SLM that normalizes EPCIS/X12/CSV pharma supply chain transactions into a canonical schema and triages DSCSA exceptions (qty mismatches, expired lots, recalls) with cited rationale, served locally via vLLM with confidence-based routing to a frontier model for hard cases.
